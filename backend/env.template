# =============================================================================
# CRM Chatbot - Environment Configuration Template
# =============================================================================
# 
# Instructions:
# 1. Copy this file to create your .env file:
#    cp env.template .env
# 
# 2. Replace all <placeholder> values with your actual configuration
# 
# 3. For MongoDB Atlas:
#    - Get your connection string from MongoDB Atlas dashboard
#    - Click "Connect" → "Connect your application" → Copy connection string
#    - Replace <username>, <password>, and <cluster> with your values
# 
# 4. IMPORTANT: URL encode special characters in password!
#    @ → %40, ! → %21, # → %23, $ → %24, % → %25
# 
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------

# For MongoDB Atlas (Cloud - Recommended):
MONGO_URL=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# For Local MongoDB (Docker):
# MONGO_URL=mongodb://localhost:27017

# Database name
DB_NAME=crm_db

# -----------------------------------------------------------------------------
# SECURITY CONFIGURATION
# -----------------------------------------------------------------------------

# JWT Secret Key - CHANGE THIS IN PRODUCTION!
# Generate a secure random string (min 32 characters)
# Example generation: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your-secret-key-change-in-production-use-random-string

# JWT Algorithm (Leave as HS256 unless you know what you're doing)
ALGORITHM=HS256

# Access token expiration in minutes (default: 1440 = 24 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------

# Server host (use 0.0.0.0 to allow external access)
HOST=127.0.0.1

# Server port
PORT=8000

# -----------------------------------------------------------------------------
# CORS CONFIGURATION
# -----------------------------------------------------------------------------

# Allowed origins for CORS (* allows all, comma-separated for multiple)
# Production example: http://localhost:3000,https://yourapp.com
CORS_ORIGINS=*

# -----------------------------------------------------------------------------
# DEVELOPMENT FLAGS
# -----------------------------------------------------------------------------

# Skip database connection check on startup (for testing)
SKIP_DB_CHECK=false

# =============================================================================
# EXAMPLE CONFIGURATIONS
# =============================================================================
#
# Development (Local MongoDB):
# ----------------------------
# MONGO_URL=mongodb://localhost:27017
# DB_NAME=crm_db_dev
# HOST=127.0.0.1
# PORT=8000
#
# Production (MongoDB Atlas):
# ---------------------------
# MONGO_URL=mongodb+srv://crm_admin:SecurePass123@crm-cluster.abc123.mongodb.net/?retryWrites=true&w=majority
# DB_NAME=crm_db
# JWT_SECRET_KEY=a4f8d9e2b3c1a5f7e9d2c4b6a8e1f3d5c7b9a2e4f6d8c1b3a5e7f9d2c4b6a8
# HOST=0.0.0.0
# PORT=8000
# CORS_ORIGINS=https://myapp.com,https://www.myapp.com
#
# =============================================================================

